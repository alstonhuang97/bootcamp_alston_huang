# Hobby-Box EV Calculator — environment (Stage 02: tooling setup).
# Pinned from the project .venv (Python 3.14). Grows as later stages add libraries.
python-dotenv==1.2.3
numpy==2.5.2
ipykernel==7.3.0
